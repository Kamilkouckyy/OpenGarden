# OpenGarden Deployment Backlog

This backlog follows the spreadsheet template from the provided screenshot.


| Backlog Request                     | Tag | Task                                   | Sprint | Estimation (in hours) | Solver       | Status  | Worked Hours | Comment                                                                                                                                                                                                                                                                                           |
| ----------------------------------- | --- | -------------------------------------- | ------ | --------------------- | ------------ | ------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Deployment - Repository Preparation | ANA | Prepare repository for deployment      | 1      | 1                     | Jakub Ryba   | Waiting |              | Check that the deployment branch contains all required code, `package.json` scripts, `package-lock` files, migration files, `DEPLOYMENT.md`, and no local `.env` secrets. Confirm the repository is pushed to GitHub so Render and Vercel can import it.                                          |
| Deployment - Repository Preparation | ANA | Verify build commands locally          | 1      | 2                     | Jakub Ryba   | Waiting |              | Run the same commands that hosting will use before configuring services: `npm run build:backend` from repository root and `npm --prefix frontend run build`. Fix any build error before creating Render or Vercel services.                                                                       |
| Deployment - Database               | ANA | Create Neon PostgreSQL project         | 1      | 1                     | Kamil Koucky | Waiting |              | Create a free Neon project at [https://neon.tech](https://neon.tech). Create or use the default database for OpenGarden. Copy the pooled PostgreSQL connection string with `sslmode=require`. This value will be used as `DATABASE_URL` in Render and must not be committed to git.               |
| Deployment - Database               | ANA | Verify database migrations             | 1      | 1                     | Kamil Koucky | Waiting |              | After Render starts the backend, verify that the migration command created the required tables in Neon. In Neon SQL Editor check application tables such as `users`, `garden_beds`, `tasks`, `reports`, `equipment`, `community_events`, and `event_participations`.                              |
| Deployment - Backend Hosting        | ANA | Create Render Web Service              | 1      | 1                     | Kamil Koucky | Waiting |              | In Render create a new Web Service from the OpenGarden GitHub repository. Use Node runtime, repository root as root directory, Node version 20, build command `npm run build:backend`, start command `npm run start:backend`, and health check path `/api/docs`.                                  |
| Deployment - Backend Config         | ANA | Configure Render environment variables | 1      | 1                     | Kamil Koucky | Waiting |              | In Render service Environment tab add `DATABASE_URL` with the Neon pooled connection string. Also set `NODE_ENV=production`, `PORT=3000`, `CORS_ORIGINS=https://<vercel-frontend-url>`, and any backend runtime values required by the app. Do not paste these values into `backend/.env` in git. |
| Deployment - Backend Verification   | ANA | Verify Render backend startup          | 1      | 1                     | Kamil Koucky | Waiting |              | Open Render Logs and confirm that `npm run start:backend` runs `db:migrate` and then starts NestJS without errors. Open `https://<render-backend-url>/api/docs` and confirm Swagger loads. First request may be slow because Render free service can sleep.                                       |
| Deployment - Frontend Hosting       | ANA | Create Vercel frontend project         | 1      | 1                     | Lukas Jancik | Waiting |              | In Vercel import the OpenGarden GitHub repository. Set Framework Preset to Create React App, Root Directory to `frontend`, Install Command to `npm install`, Build Command to `npm run build`, Output Directory to `build`, and Node version to 20.                                               |
| Deployment - Frontend Config        | ANA | Configure Vercel backend URL           | 1      | 1                     | Lukas Jancik | Waiting |              | In Vercel Project Settings > Environment Variables add `REACT_APP_API_BASE_URL` with the deployed Render backend origin, for example `https://opengarden-api.onrender.com`. Redeploy the frontend because Create React App embeds env values during build.                                        |
| Deployment - Frontend Verification  | ANA | Verify Vercel frontend startup         | 1      | 1                     | Lukas Jancik | Waiting |              | Open the Vercel frontend URL and confirm the OpenGarden app loads. Verify browser network requests point to the Render backend URL from `REACT_APP_API_BASE_URL` and not localhost.                                                                                                               |
| Deployment - App Smoke Test         | ANA | Verify deployed application flow       | 1      | 2                     | Lukas Jancik | Waiting |              | Open the deployed frontend and test the main screens: garden beds, tasks, reports, equipment, and events. Confirm pages do not show failed fetch, CORS, or localhost errors. Create or update one small test item if the UI allows it and verify it persists after page refresh.                  |
| Deployment - Release                | ANA | Final deployment sign-off              | 1      | 1                     | Jakub Ryba   | Waiting |              | Confirm repository preparation, Render backend, Neon database, and Vercel frontend tasks are complete. Record final production URLs for frontend and backend. Take screenshots of the deployed frontend and at least one successful API-backed page for project submission.                       |
