{
  "name": "opengarden",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "build:backend": "npm install --prefix backend && npm --prefix backend run build",
    "migrate:backend": "npm --prefix backend run db:migrate",
    "start:backend": "npm run migrate:backend && npm --prefix backend run start:prod",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "better-auth": "^1.6.9"
  }
}
